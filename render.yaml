services:
  - type: web
    name: google-map-scraper
    runtime: python312
    plan: free
    
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    
    startCommand: gunicorn mapscraper.wsgi:application
    
    envVars:
      - key: DEBUG
        value: false
      - key: PYTHON_VERSION
        value: "3.12.0"
    
    # staticPublicPath: /static
    # staticDir: staticfiles
